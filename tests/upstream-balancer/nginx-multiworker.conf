daemon off;
worker_processes 2;
error_log logs/error.log debug;
pid logs/nginx.pid;

events {
    worker_connections 64;
}

http {
    access_log logs/access.log;
    variables_hash_max_size 2048;
    variables_hash_bucket_size 128;

    # Two backends — needed to prove each worker resolves the same key to the
    # same peer, not just that any one backend was chosen.
    upstream backend_two {
        upstream_balancer_sticky_cookie route;
        upstream_balancer_fallback next;

        server 127.0.0.1:19002;
        server 127.0.0.1:19003;
    }

    server {
        listen 8888;

        location / {
            proxy_pass http://backend_two;
        }
    }
}
