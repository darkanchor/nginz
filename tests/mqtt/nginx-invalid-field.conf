daemon off;
error_log logs/error.log debug;
pid logs/nginx.pid;

events {
    worker_connections 64;
}

stream {
    server {
        listen 18887;
        mqtt on;
        mqtt_set_connect topic "unsupported";
        proxy_pass 127.0.0.1:18884;
    }
}
